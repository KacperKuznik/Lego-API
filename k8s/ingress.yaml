apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lego-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lego-api
            port:
              number: 80
